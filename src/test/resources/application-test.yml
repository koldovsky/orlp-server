spring:
  datasource:
#    url: jdbc:h2:mem:testdb;INDB_CLOSE_ON_EXIT=FALSE;IT=CREATE SCHEMA IF NOT EXISTS PUBLIC
#    ;INIT=create schema if not exists orlp;
#    ;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
    url: jdbc:h2:mem:orlp;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
    platform: h2
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    database: h2
  h2:
    console:
      enabled: true
      path: /console
#  autoconfigure:
#    exclude: org.springframework.boot.autoconfigure.security.SecurityAutoConfiguration
#    exclude: org.springframework.boot.autoconfigure.security.SecurityFilterAutoConfiguration,org.springframework.boot.autoconfigure.security.FallbackWebSecurityAutoConfiguration,org.springframework.boot.autoconfigure.security.oauth2.OAuth2AutoConfiguration
#security:
#  user:
#    name: user
#    password: password
#    role: USER
flyway:
  baseline-on-migrate: true
  baseline-version: 1.0